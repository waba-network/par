## Compiling

- Be sure to clone and build EOS

- Create the build output dir

  ```
  mkdir build
  cd build

  ```

- CMake

  ```
  cmake -DEOS_HOME=/path/to/your/eos/root ..
  ```

- Make

  ```
  make
  ```  
  

